import { RightClickPreventDirective } from './right-click-prevent.directive';

describe('RightClickPreventDirective', () => {
  it('should create an instance', () => {
    const directive = new RightClickPreventDirective();
    expect(directive).toBeTruthy();
  });
});
